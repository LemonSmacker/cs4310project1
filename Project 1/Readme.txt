All of these programs were written in Java and were ran through the command prompt window using JDK. I could not get them all to run correctly in my IDE so I recommend this as well.

Assuming JDK is installed, type "java [file name]" to execute each respective file.